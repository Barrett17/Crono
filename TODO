* Add file LICENSE, creator and license of the sounds

* Finish tempo slider.

* Aggiungere una gestione decente degli accenti, in modo da poterli selezionare
  su una battuta qualsiasi, l'idea è quella di fornire degli altri checkbox che indicano la 
  battuta e che cambiano quando cambia il tempo, ad esempio in 2/4 avremo due checkbox
  , in 3/4  tre  e così via, quindi selezionando uno o più checkbox l'accento verrà 
  eseguito sulla battuta selezionata. 

* Create format for soundpacks, it cpuld be simply a text file
  that defines pairs of sounds.

* Creare la lancetta del metronomo che oscilla con le misure sotto.

* Make it replicantable (if possible).

* Improve settings with BFilePanel

* Profiles support (?!?!)
